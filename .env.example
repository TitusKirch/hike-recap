# Environment template. Copy to .env for local overrides (.env is gitignored).
#
# No map API key is required: the map is drawn from local geodata rather than
# fetched from a tile provider. See CLAUDE.md -> "The map is drawn, not fetched".
#
# Kept as a placeholder so a future layer that does need a key has a home.
