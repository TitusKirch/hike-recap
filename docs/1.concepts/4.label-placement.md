---
title: 'Label placement'
description: 'Places and peaks are placed by a pure function against three ordered rules, and a peak that cannot satisfy them is dropped rather than shifted.'
---

# Label placement

Overlapping labels were the most visible defect in every prototype, so placement is a pure function with its own tests: `placeLabels()` in `src/util/labels.ts`.

## How it works

Labels follow three rules, in order:

1. never overlap another label
2. never sit on a route
3. never leave the safe area

Each label is tried against a set of candidate offsets and the first that satisfies all three wins. Where a **place** cannot satisfy all three it is kept anyway — the halo carries it, and places are what orient the viewer. Where a **peak** cannot, it is dropped: a peak that cannot be placed cleanly is worth less than a clean frame.

Only climbed summits are labelled, and they are matched to a track **by coordinate**, resolved once at ingest.

## Why it's built this way

Three traps, each hit at least once and each silent:

- **Every summit marker is an obstacle, not just a label's own.** `placeLabels` keeps a label off the marker it belongs to; without adding the other markers, one peak's name lands on a neighbour's triangle.
- **A label's bounding box must cover its elevation line.** A peak label is a name _and_ a `1234 m` line underneath. Sizing the box from the name alone is exactly how routes end up drawn through the elevation text, so `LabelInput` takes explicit `above` / `below` rather than one height.
- **Match summits by coordinate, never by name.** Peak names repeat within a region — a dozen did in the first area this ran on. Name matching labelled every peak sharing a name with a climbed one, and printed one peak's elevation on another.

**Lake names follow the lake.** Set level, a name only fits a lake that happens to be _wide_; rotated onto the outline's principal axis — `principalAxis()`, a closed-form 2 × 2 PCA — it fits whenever the lake is _long_, which most are. Where even that does not fit (a long lake can still be only 70 px against a 133 px name) the name moves beside the outline, and there it may only go directly above or below. Allowed to slide sideways like a place name, it drifts far enough to read as naming the _neighbouring_ lake, which is worse than leaving it off.
